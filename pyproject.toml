[project]
name = "db-sentinel"
version = "0.1.0"
description = "Automated MySQL backup, verification and monitoring for on-premise environments"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
